# Core dependencies
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# AI Accelerators SDK
# Note: In production, use: accelerators>=0.1.0
# For local development, install from source:
# pip install -e /path/to/ai-accelerators/sdk

# LLM Providers
{% if values.llmProvider == 'anthropic' %}
anthropic>=0.20.0
{% elif values.llmProvider == 'openai' %}
openai>=1.10.0
{% elif values.llmProvider == 'google' %}
google-generativeai>=0.3.0
{% endif %}

# Embedding Providers
{% if values.embeddingProvider == 'openai' %}
openai>=1.10.0
{% elif values.embeddingProvider == 'voyage' %}
voyageai>=0.2.0
{% elif values.embeddingProvider == 'cohere' %}
cohere>=4.0.0
{% endif %}

# Vector Store
{% if values.vectorStore == 'qdrant' %}
qdrant-client>=1.7.0
{% elif values.vectorStore == 'pinecone' %}
pinecone-client>=3.0.0
{% elif values.vectorStore == 'chromadb' %}
chromadb>=0.4.0
{% endif %}

# Observability
langfuse>=2.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
